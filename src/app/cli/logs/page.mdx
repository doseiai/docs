export const metadata = {
  title: 'dplex logs',
  description:
    'Learn how to stream your project logs using dplex logs CLI command.',
}


# dplex logs
Learn how to stream your project logs using dplex logs CLI command.

## dplex logs
```bash
dplex logs --owner <username>/<project_name>
```
Using this command, you can stream the logs of your project's application.
- `--owner` is required
