import { Button } from '@/components/Button'

export const metadata = {
  title: 'Dosei CLI Overview',
  description:
    'Learn how to use the Dosei command-line interface (CLI) to manage and configure your Dosei Projects from the command line.',
}


# Dosei CLI
Learn how to use `dctl` the Dosei command-line interface (CLI) to manage and configure your Dosei Projects from the command line.


**macOS**

If you have the [Homebrew](https://brew.sh) package manager installed, `dctl` can be installed by running:

```bash
brew install doseiai/tap/dctl
```

If not, you can run the install script:

```bash
curl -fsSL https://dosei.ai/install.sh | sh
```

**Linux**

From the terminal, run:

```bash
curl -fsSL https://dosei.ai/install.sh | sh
```

**Windows**

From powershell, run:

```powershell
irm https://dosei.ai/install.ps1 | iex
```

## Before you begin

To start using the CLI, you will first need to login into your Dosei account.

<div className="not-prose">
  <Button
    href="/cli/login"
    variant="text"
    arrow="right"
    children="dctl login"
  />
</div>
