export const metadata = {
  title: 'Install Dosei on Linux or macOS',
  description: 'How to install Dosei on Linux or macOS',
}

# Install Dosei on Linux or macOS

How to install Dosei on Linux or macOS

## Install

From the terminal, run:

```bash
curl -fsSL https://dosei.ai/install.sh | sh
```

For a more complete installation, continue with [these instructions](/getting-started).
